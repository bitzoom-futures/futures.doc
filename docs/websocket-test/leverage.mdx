---
title: Leverage Channel Test
sidebar_label: /api/v1/leverage
---

import { WebSocketTester, getChannel } from '@site/src/components/WebSocketTester'

`/api/v1/leverage` is private. Connect, run `logon`, then subscribe with optional `{ symbol }`.

<WebSocketTester channel={getChannel('leverage')} />
